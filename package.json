{
  "name": "blood-donor-search-db",
  "version": "0.0.0",
  "private": true,
  "description": "Database tooling (Supabase CLI, migrations, seed) — the Next.js app lives in frontend/.",
  "scripts": {
    "db:migrate": "supabase migration up --local",
    "db:seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "pg": "^8",
    "supabase": "^2"
  }
}
